# Optional: Anthropic API key for AI-generated insights.
# If unset, the backend falls back to template-based insights.
ANTHROPIC_API_KEY=your-key-here
# Optional: override the default model
ANTHROPIC_MODEL=claude-sonnet-4-6
